export enum EmailVerificationStatus {
  CONFIRMED = 'CONFIRMED',
  FAILED = 'FAILED',
}
export enum PasswordResetStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

export enum PasswordUpdateStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}
