// import { Box } from '@mui/material';
// import { useGenerate2fa } from 'api';
import { FC } from 'react';

export const TwoFactorAuthQrCode: FC = () => {
  // const { data } = useGenerate2fa();

  // TODO JAVA MIGRATION: This is a functionality currently not supported. Commented it out for the time being.
  // const base64ImageString = Buffer.from(data, 'binary').toString('base64');

  return (
    // TODO JAVA MIGRATION: This is a functionality currently not supported. Commented it out for the time being.
    // <Box sx={{ display: 'flex', justifyContent: 'center', mb: 2 }}>
    //   <img src={`data:image/png;base64,${base64ImageString}`} alt="Two Factor QR code" />
    // </Box>
    <></>
  );
};
