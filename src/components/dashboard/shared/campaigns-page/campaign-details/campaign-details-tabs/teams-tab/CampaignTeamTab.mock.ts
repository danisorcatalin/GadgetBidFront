import { campaignMembersMock } from 'mocks';
import type { CampaignTeamTabProps } from './CampaignTeamTab';

export const mockCampaignTeamTabProps: CampaignTeamTabProps = {
  campaignMembers: campaignMembersMock,
  readonly: false,
};
